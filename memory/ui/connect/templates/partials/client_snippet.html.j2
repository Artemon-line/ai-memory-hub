<article>
  <h3>{{ client.name|e }}</h3>
  <p>{{ client.status|e }}</p>
  <textarea id="{{ client.element_id|e }}" readonly rows="4">{{ client.snippet|e }}</textarea>
  <button type="button" data-copy="{{ client.element_id|e }}">Copy</button>
</article>
