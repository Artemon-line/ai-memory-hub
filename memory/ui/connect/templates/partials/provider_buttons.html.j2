{% if providers %}
  {% for provider in providers %}
    <a class="button" href="{{ provider.href|e }}">Sign In With {{ provider.label|e }}</a>
  {% endfor %}
{% else %}
  <p class="muted">No sign-in providers are enabled.</p>
{% endif %}
